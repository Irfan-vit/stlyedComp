import styled from 'styled-components'

const NavBar = () => {
    return(
        <header>
            <nav>
                <form action="">
                    <label htmlFor="categories">
                        <select name="" id=""></select>
                    </label>
                </form>
            </nav>
        </header>
    )
}