import styled from 'styled-components'

const NavBar = () => {
  return (
    <header>
      <nav>
        <form action="">
          <label htmlFor="categories">
            <select name="cat1" id="cat1"></select>
          </label>
        </form>
      </nav>
    </header>
  )
}
