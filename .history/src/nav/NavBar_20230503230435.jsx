import styled from 'styled-components'

const NavBar = () => {
    return(
        <header>
            <nav>
                <form action="">
                    <label htmlFor="categories">
                        <select name="cat1" id=""></select>
                    </label>
                </form>
            </nav>
        </header>
    )
}